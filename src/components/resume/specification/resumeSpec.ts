/**
 * Sprint 12: the resume's page geometry (A4 at 96dpi) — the one remaining
 * consumer is ResumeScene.tsx, which uses it to compute the floating paper
 * mesh's aspect ratio. The rest of the Document Specification (typography,
 * spacing, color, divider styling) existed only for the HTML/html2canvas
 * rendering pipeline removed this sprint and was removed along with it.
 */
export const RESUME_PAGE_WIDTH_PX = 794;
export const RESUME_PAGE_HEIGHT_PX = 1123;
